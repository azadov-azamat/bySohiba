import {BASE_URL} from "./constants"

const config = {
    language: {
        key: "language",
        initial: "ru",
        list: ["uz", "ru", "en"]
    },
    api: {
        base_url: BASE_URL
    }
};

export default config;